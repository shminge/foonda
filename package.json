{
  "devDependencies": {
    "@types/node": "^24.0.4",
    "gh-pages": "^6.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && gh-pages -d dist"
  }
}
